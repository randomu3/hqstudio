{
  "enabled": true,
  "name": "Запуск тестов API",
  "description": "При сохранении C# файла в папке HQStudio.API автоматически запускает тесты и показывает результат",
  "version": "1",
  "when": {
    "type": "fileEdited",
    "patterns": [
      "HQStudio.API/**/*.cs"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Запусти команду `dotnet test HQStudio.API.Tests --verbosity minimal` и покажи результат выполнения тестов."
  }
}