<UserControl x:Class="HQStudio.Views.ServicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Style="{DynamicResource TextH1}">
                <TextBlock.Effect>
                    <DropShadowEffect Color="White" BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
                </TextBlock.Effect>
                УСЛУГИ
            </TextBlock>
            <TextBlock Text="Управление каталогом услуг • Двойной клик для редактирования" 
                       Style="{DynamicResource TextBody}" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{DynamicResource BgHoverBrush}" CornerRadius="8" Padding="12,8" Margin="0,0,12,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Всего: " FontSize="13" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalServices}" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Button Content="🔄" Style="{DynamicResource SecondaryButton}" Padding="8" MinWidth="36" Height="36"
                        Command="{Binding RefreshCommand}" ToolTip="Обновить"/>
            </StackPanel>

            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{DynamicResource InputSearch}" HorizontalAlignment="Left" Margin="12,0,0,0"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="🗑️ Удалить" Style="{DynamicResource BtnDanger}" Margin="0,0,12,0"
                        Command="{Binding DeleteServiceCommand}"/>
                <Button Content="+ Добавить услугу" Style="{DynamicResource BtnPrimary}"
                        Command="{Binding AddServiceCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Loading Overlay -->
        <Border Grid.Row="2" Background="#80000000" CornerRadius="16" Panel.ZIndex="100"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Border Width="60" Height="60" CornerRadius="30" Background="{DynamicResource BgIconBrush}" Margin="0,0,0,16">
                    <TextBlock Text="⏳" FontSize="28" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.RenderTransform>
                            <RotateTransform x:Name="LoadingRotate" CenterX="14" CenterY="14"/>
                        </TextBlock.RenderTransform>
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation Storyboard.TargetProperty="(TextBlock.RenderTransform).(RotateTransform.Angle)"
                                                         From="0" To="360" Duration="0:0:1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                </Border>
                <TextBlock Text="Загрузка услуг..." FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>

        <!-- Services List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,16,0,0">
            <ItemsControl ItemsSource="{Binding Services}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{DynamicResource CardHoverable}" Margin="0,0,0,12"
                                MouseLeftButtonDown="Service_MouseLeftButtonDown">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" Width="64" Height="64" CornerRadius="14" 
                                        Background="{DynamicResource BgIconBrush}" Margin="0,0,20,0">
                                    <TextBlock Text="{Binding Icon}" FontSize="32" Foreground="{DynamicResource TextPrimaryBrush}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" 
                                               Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                                    <Border Style="{DynamicResource Badge}" HorizontalAlignment="Left" Margin="0,0,0,8">
                                        <TextBlock Text="{Binding Category}" FontSize="11" Foreground="{DynamicResource TextMutedBrush}"/>
                                    </Border>
                                    <TextBlock Text="{Binding Description}" Style="{DynamicResource TextSmall}"
                                               TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="36"/>
                                </StackPanel>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" MinWidth="120">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="от " FontSize="12" Foreground="{DynamicResource TextMutedBrush}"/>
                                        <TextBlock Text="{Binding PriceFrom, StringFormat={}{0:N0}}" 
                                                   FontSize="24" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                        <TextBlock Text=" ₽" FontSize="12" Foreground="{DynamicResource TextMutedBrush}" 
                                                   VerticalAlignment="Bottom" Margin="0,0,0,4"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding Services.Count, Converter={StaticResource CountToVisibility}}">
            <Border Width="80" Height="80" CornerRadius="40" Background="{DynamicResource BgIconBrush}" Margin="0,0,0,20">
                <TextBlock Text="🔧" FontSize="36" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="Услуг пока нет" Style="{DynamicResource TextH3}" HorizontalAlignment="Center"/>
            <TextBlock Text="Добавьте первую услугу" Style="{DynamicResource TextSmall}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
