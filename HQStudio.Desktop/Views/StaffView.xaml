<UserControl x:Class="HQStudio.Views.StaffView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:HQStudio.Controls">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Style="{DynamicResource TextH1}">
                <TextBlock.Effect>
                    <DropShadowEffect Color="White" BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
                </TextBlock.Effect>
                СОТРУДНИКИ
            </TextBlock>
            <TextBlock Text="Управление командой • Двойной клик для редактирования" 
                       Style="{DynamicResource TextBody}" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Style="{DynamicResource CounterBadge}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Всего: " FontSize="13" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Users.Count}" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Button Content="🔄 Обновить" Style="{DynamicResource SecondaryButton}" Margin="0,0,12,0"
                        Command="{Binding RefreshCommand}"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="🔑 Сбросить пароль" Style="{DynamicResource BtnSecondary}" Margin="0,0,12,0"
                        Command="{Binding ResetPasswordCommand}" ToolTip="Сбросить пароль выбранного сотрудника"/>
                <Button Content="⏸️ Деактивировать" Style="{DynamicResource BtnSecondary}" Margin="0,0,12,0"
                        Command="{Binding ToggleActiveCommand}"/>
                <Button Content="🗑️ Удалить" Style="{DynamicResource BtnDanger}" Margin="0,0,12,0"
                        Command="{Binding DeleteUserCommand}"/>
                <Button Content="+ Добавить сотрудника" Style="{DynamicResource BtnPrimary}"
                        Command="{Binding AddUserCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Loading (только когда есть подключение) -->
        <TextBlock Grid.Row="2" Text="Загрузка..." FontSize="16" Foreground="#707070"
                   HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsLoading}" Value="True"/>
                                <Condition Binding="{Binding IsApiConnected}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <!-- No API Connection -->
        <controls:NoApiConnectionView Grid.Row="2" SectionName="Сотрудники"
                                      Visibility="{Binding IsApiConnected, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

        <!-- Staff List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,16,0,0">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsApiConnected}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            <ItemsControl ItemsSource="{Binding Users}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{DynamicResource ListRowCard}"
                                MouseLeftButtonDown="User_MouseLeftButtonDown"
                                Cursor="Hand"
                                Opacity="{Binding IsActive, Converter={StaticResource BoolToOpacityConverter}}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Selection Indicator -->
                                <Border x:Name="SelectionIndicator" Grid.Column="0" Width="4" CornerRadius="2" Margin="0,4,12,4"
                                        VerticalAlignment="Stretch" Background="Transparent"/>

                                <!-- Avatar -->
                                <Border Grid.Column="1" Style="{DynamicResource ListRowIcon}">
                                    <TextBlock Text="👤" FontSize="24" Foreground="{DynamicResource TextPrimaryBrush}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <!-- Info -->
                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" 
                                               Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Login, StringFormat=@{0}}" Style="{DynamicResource TextSmall}" Margin="0,0,16,0"/>
                                        <Ellipse Width="8" Height="8" Margin="0,0,6,0" VerticalAlignment="Center" Fill="{Binding StatusColor}"/>
                                        <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{Binding StatusColor}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Role Badge -->
                                <Border Grid.Column="3" CornerRadius="6" Padding="12,6" Margin="0,0,16,0" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="#2A2A2A"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Role}" Value="Admin">
                                                    <Setter Property="Background" Value="#2A2A1A"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding RoleDisplay}" FontSize="11" FontWeight="Medium">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#909090"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Role}" Value="Admin">
                                                        <Setter Property="Foreground" Value="#FFC107"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>

                                <!-- Created Date -->
                                <TextBlock Grid.Column="4" Text="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" 
                                           Style="{DynamicResource TextSmall}" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
