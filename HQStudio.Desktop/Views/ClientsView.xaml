<UserControl x:Class="HQStudio.Views.ClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:HQStudio.Controls">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Style="{DynamicResource TextH1}">
                <TextBlock.Effect>
                    <DropShadowEffect Color="White" BlurRadius="15" ShadowDepth="0" Opacity="0.2"/>
                </TextBlock.Effect>
                КЛИЕНТЫ
            </TextBlock>
            <TextBlock Text="База клиентов • Двойной клик для редактирования" 
                       Style="{DynamicResource TextBody}" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Toolbar -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Style="{DynamicResource CounterBadge}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Всего: " FontSize="13" Foreground="{DynamicResource TextMutedBrush}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalClients}" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Button Content="🔄 Обновить" Style="{DynamicResource SecondaryButton}" Margin="0,0,12,0"
                        Command="{Binding RefreshCommand}"/>
            </StackPanel>

            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Style="{DynamicResource InputSearch}" HorizontalAlignment="Left" Margin="12,0,0,0"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="📊 Excel" Style="{DynamicResource SecondaryButton}" Margin="0,0,12,0"
                        Command="{Binding ExportToExcelCommand}" ToolTip="Экспорт в Excel"/>
                <Button Content="🗑️ Удалить" Style="{DynamicResource BtnDanger}" Margin="0,0,12,0"
                        Command="{Binding DeleteClientCommand}"/>
                <Button Content="+ Добавить клиента" Style="{DynamicResource BtnPrimary}"
                        Command="{Binding AddClientCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Loading Overlay (только когда есть подключение) -->
        <Border Grid.Row="2" Background="{DynamicResource BgOverlayBrush}" CornerRadius="16" Panel.ZIndex="100">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding IsLoading}" Value="True"/>
                                <Condition Binding="{Binding IsApiConnected}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" Value="Visible"/>
                        </MultiDataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="Загрузка клиентов..." FontSize="14" Foreground="{DynamicResource TextSecondaryBrush}"
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Border>

        <!-- No API Connection -->
        <controls:NoApiConnectionView Grid.Row="2" SectionName="Клиенты"
                                      Visibility="{Binding IsApiConnected, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>

        <!-- Clients List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,16,0,0">
            <ScrollViewer.Style>
                <Style TargetType="ScrollViewer">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsApiConnected}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ScrollViewer.Style>
            <ItemsControl ItemsSource="{Binding Clients}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{DynamicResource ListRowCard}"
                                MouseLeftButtonDown="Client_MouseLeftButtonDown"
                                Cursor="Hand">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Selection Indicator -->
                                <Border x:Name="SelectionIndicator" Grid.Column="0" Width="4" CornerRadius="2" Margin="0,4,12,4"
                                        VerticalAlignment="Stretch" Background="Transparent"/>

                                <Border Grid.Column="1" Style="{DynamicResource ListRowIcon}">
                                    <TextBlock Text="👤" FontSize="24" Foreground="{DynamicResource TextPrimaryBrush}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" 
                                               Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,4"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Phone}" Style="{DynamicResource TextSmall}" Margin="0,0,16,0"/>
                                        <TextBlock Text="{Binding Car}" Style="{DynamicResource TextSmall}" Foreground="#909090"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Column="3" VerticalAlignment="Center" HorizontalAlignment="Right">
                                    <Border Background="{DynamicResource BgIconBrush}" CornerRadius="6" Padding="10,5" Margin="0,0,0,4">
                                        <TextBlock Text="{Binding CarNumber}" FontSize="13" Foreground="{DynamicResource TextPrimaryBrush}" 
                                                   FontFamily="Consolas" FontWeight="SemiBold"/>
                                    </Border>
                                    <TextBlock Text="{Binding CreatedAt, StringFormat=dd.MM.yyyy}" 
                                               Style="{DynamicResource TextSmall}" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Empty State -->
        <StackPanel Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Center"
                    Visibility="{Binding ShowEmptyState, Converter={StaticResource BoolToVisibility}}">
            <Border Width="80" Height="80" CornerRadius="40" Background="{DynamicResource BgIconBrush}" Margin="0,0,0,20">
                <TextBlock Text="👥" FontSize="36" Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <TextBlock Text="Клиентов пока нет" Style="{DynamicResource TextH3}" HorizontalAlignment="Center"/>
            <TextBlock Text="Добавьте первого клиента" Style="{DynamicResource TextSmall}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Pagination -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0"
                    Visibility="{Binding TotalPages, Converter={StaticResource GreaterThanOneToVisibility}, FallbackValue=Collapsed}">
            <Button Content="◀ Назад" Command="{Binding PreviousPageCommand}" 
                    Style="{DynamicResource SecondaryButton}" Margin="0,0,10,0"/>
            
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" 
                       Margin="10,0,10,0" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
            
            <Button Content="Вперёд ▶" Command="{Binding NextPageCommand}" 
                    Style="{DynamicResource SecondaryButton}" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
