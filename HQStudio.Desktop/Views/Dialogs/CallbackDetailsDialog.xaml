<Window x:Class="HQStudio.Views.Dialogs.CallbackDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" 
        Height="680" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        KeyDown="Window_KeyDown"
        Loaded="Window_Loaded">
    
    <Border Background="{DynamicResource BgPrimaryBrush}" CornerRadius="16" 
            BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="{DynamicResource BgSecondaryBrush}" 
                    CornerRadius="16,16,0,0" Padding="24,18">
                <Grid>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📋" FontSize="20" Margin="0,0,12,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Title}" FontSize="18" FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Content="✕" Click="CloseButton_Click" Style="{DynamicResource BtnIcon}"
                            HorizontalAlignment="Right" Width="36" Height="36"/>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="24,20,24,24">
                    <!-- Status Badge -->
                    <Border Background="{Binding StatusBgColor}" CornerRadius="8" 
                            Padding="14,8" HorizontalAlignment="Left" Margin="0,0,0,24">
                        <TextBlock Text="{Binding Status}" Foreground="{Binding StatusFgColor}" 
                                   FontWeight="SemiBold" FontSize="13"/>
                    </Border>

                    <!-- Existing Client Info (if found) -->
                    <Border x:Name="ExistingClientPanel" Visibility="Collapsed"
                            Background="#1A2A1A" CornerRadius="12" 
                            BorderBrush="#4CAF50" BorderThickness="1"
                            Padding="16" Margin="0,0,0,20">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="✓" FontSize="16" Foreground="#4CAF50" Margin="0,0,8,0"/>
                                <TextBlock Text="Клиент найден в базе" FontSize="13" FontWeight="SemiBold"
                                           Foreground="#4CAF50"/>
                            </StackPanel>
                            <TextBlock x:Name="ExistingClientName" FontSize="14" 
                                       Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <TextBlock x:Name="ExistingClientInfo" FontSize="12" 
                                       Foreground="{DynamicResource TextMutedBrush}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Contact Section -->
                    <TextBlock Text="КОНТАКТНЫЕ ДАННЫЕ" Style="{DynamicResource TextLabel}" Margin="0,0,0,12"/>
                    <Border Background="{DynamicResource BgCardBrush}" CornerRadius="12" 
                            BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1"
                            Padding="18" Margin="0,0,0,24">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="👤" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold"
                                           Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📞" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Phone}" FontSize="15" Foreground="#2196F3" 
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Car Section -->
                    <TextBlock Text="АВТОМОБИЛЬ" Style="{DynamicResource TextLabel}" Margin="0,0,0,12"/>
                    <Border Background="{DynamicResource BgCardBrush}" CornerRadius="12" 
                            BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1"
                            Padding="18" Margin="0,0,0,24">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <TextBlock Text="🚗" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding CarModel, TargetNullValue='Не указан'}" FontSize="15"
                                           Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔢" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding LicensePlate, TargetNullValue='Не указан'}" FontSize="15"
                                           FontFamily="Consolas" Foreground="{DynamicResource TextMutedBrush}" 
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Message Section -->
                    <StackPanel Visibility="{Binding HasMessage, Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="СООБЩЕНИЕ" Style="{DynamicResource TextLabel}" Margin="0,0,0,12"/>
                        <Border Background="{DynamicResource BgCardBrush}" CornerRadius="12" 
                                BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1"
                                Padding="18" Margin="0,0,0,24">
                            <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="14"
                                       Foreground="{DynamicResource TextSecondaryBrush}"/>
                        </Border>
                    </StackPanel>

                    <!-- Info Section -->
                    <TextBlock Text="ИНФОРМАЦИЯ" Style="{DynamicResource TextLabel}" Margin="0,0,0,12"/>
                    <Border Background="{DynamicResource BgCardBrush}" CornerRadius="12" 
                            BorderBrush="{DynamicResource BorderDefaultBrush}" BorderThickness="1"
                            Padding="18">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Источник" Style="{DynamicResource TextSmall}" Margin="0,0,0,6"/>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding SourceIcon}" FontSize="16" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding Source}" FontSize="14"
                                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Дата создания" Style="{DynamicResource TextSmall}" Margin="0,0,0,6"/>
                                <TextBlock Text="{Binding FormattedDate}" FontSize="14"
                                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer with Actions -->
            <Border Grid.Row="2" Padding="24,18" BorderBrush="{DynamicResource BorderDefaultBrush}" 
                    BorderThickness="0,1,0,0">
                <StackPanel>
                    <!-- Create Client (only if client not found) -->
                    <Button x:Name="CreateClientBtn" Content="👤 Создать клиента" Click="CreateClient_Click" 
                            Style="{DynamicResource BtnSuccess}" Margin="0,0,0,12" Padding="20,12"/>
                    
                    <!-- Create Order (only if client exists) -->
                    <Button x:Name="CreateOrderBtn" Content="📦 Создать заказ" Click="CreateOrder_Click" 
                            Style="{DynamicResource BtnPrimary}" Margin="0,0,0,12" Padding="20,12"
                            Visibility="Collapsed"/>
                    
                    <Button Content="Закрыть" Click="CloseButton_Click" 
                            Style="{DynamicResource BtnSecondary}" Padding="20,10"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
